/* This file contains references to the vendor libraries we're using in this project.
This is used by webpack in the production build only. A separate bundle for vendor code
is useful since it's unlikely to change as often as the app code. So all of the libraries
we reference here will be written to vendor.js so they can be cached...
*/

/*eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
